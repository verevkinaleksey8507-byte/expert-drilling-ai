# Tests

Reserved for manual and automated QA assets, reference cases, fixtures and expected results.

Foundation validation is defined in `Documentation/ACCEPTANCE_CHECKLIST.md`. No calculation tests are required because version `0.1.0` contains no calculations.

Layout-stage validation is defined in `IWCF_LAYOUT_CHECKLIST.md`. Version
`0.2.0` still contains no calculations, so formula-result test cases remain
deferred.
